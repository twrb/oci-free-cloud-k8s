apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lychee-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/ingress.class: nginx
    external-dns.alpha.kubernetes.io/hostname: img.tb10.dpdns.org
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  tls:
    - hosts:
        - img.tb10.dpdns.org
      secretName: lychee-cert
  rules:
    - host: img.tb10.dpdns.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lychee-service
                port:
                  name: http
